<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The row containing the counter is selected -->
    <item android:state_activated="true" android:drawable="@drawable/drawer_item_counter_bg_selected" />
    <!-- The row containing the counter is normal (ie not selected, not focused, ...) -->
    <item android:drawable="@drawable/drawer_item_counter_bg_default" />
</selector>
