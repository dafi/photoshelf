<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
      xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- AppCompat ignores showAsAction="never" in action mode menus, to fix this issue is necessary to specify both showAsAction -->
    <item
        android:id="@+id/group_menu_image_dimension"
        android:icon="@drawable/ic_action_show_image"
        android:showAsAction="never"
        app:showAsAction="never"
        android:title="@string/menu_show_image"
        tools:ignore="AppCompatResource">
        <menu />
    </item>
    <item
        android:id="@+id/show_post"
        android:icon="@drawable/ic_open_in_browser_white_24dp"
        android:showAsAction="never"
        app:showAsAction="never"
        android:title="@string/show_post"
        tools:ignore="AppCompatResource">
    </item>
    <item
        android:id="@+id/post_edit"
        android:icon="@drawable/ic_action_edit"
        android:title="@string/edit_post_title">
    </item>
    <item
        android:id="@+id/post_delete"
        android:icon="@drawable/ic_action_delete"
        android:title="@string/delete_post">
    </item>

</menu>