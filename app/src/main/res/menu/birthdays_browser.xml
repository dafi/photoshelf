<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <group android:id="@+id/menu_photo_action_bar" >
        <group android:checkableBehavior="single">
            <item
                android:id="@+id/action_show_all"
                android:checked="true"
                android:title="@string/all"/>
            <item
                android:id="@+id/action_show_ignored"
                android:title="@string/show_ignored"/>
            <item
                android:id="@+id/action_show_birthdays_in_same_day"
                android:title="@string/show_birthdays_in_same_day"/>
        </group>
    </group>

</menu>